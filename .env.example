VITE_ALLOWED_EMAIL_DOMAINS=mta.ca,umoncton.ca
# Backend URL (empty = localStorage only)
VITE_APPS_SCRIPT_URL=
# Proxy mode: set to true when script.google.com is blocked (e.g. school WiFi).
# Uses /.netlify/functions/sheets (same origin). Requires APPS_SCRIPT_URL on Netlify.
VITE_USE_PROXY=
